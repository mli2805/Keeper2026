<Window x:Class="KeeperWpf.ToDoView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KeeperWpf"
        xmlns:cal="http://www.caliburnproject.org"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        d:DataContext="{d:DesignInstance local:ToDoViewModel}"
        Height="850" Width="1300">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <Style TargetType="DataGridRow">
            <Style.Resources>
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#B6E6FF"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="#B6E6FF"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="Black"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="Black"/>
            </Style.Resources>
        </Style>
    </Window.Resources>
    <DockPanel Margin="5">
        <DockPanel DockPanel.Dock="Top" Margin="0,0,0,5">
            <StackPanel DockPanel.Dock="Left" Orientation="Horizontal">
                <TextBlock Text="Поиск:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                         Width="220" Height="24" Margin="0,0,10,0"/>
                <CheckBox Content="Показывать завершенные" IsChecked="{Binding ShowCompleted}"
                          VerticalAlignment="Center" Margin="0,0,20,0"/>
                <TextBlock Text="Категория:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <RadioButton Content="Дача" GroupName="TodoCategory"
                             IsChecked="{Binding IsCountryHouseSelected}"
                             VerticalAlignment="Center" Margin="0,2,10,0"/>
                <RadioButton Content="Общие" GroupName="TodoCategory"
                             IsChecked="{Binding IsCommonSelected}"
                             VerticalAlignment="Center" Margin="0,2,0,0"/>
            </StackPanel>
        </DockPanel>

        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,5,0,0">
            <Button Content="Сохранить" Name="SaveTask" IsEnabled="{Binding CanSaveTask}" Width="90" Height="24"/>
            <Button Content="Отмена" Name="CancelTask" IsEnabled="{Binding CanCancelTask}" Width="90" Height="24" Margin="5,0,0,0"/>
        </StackPanel>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="5"/>
                <RowDefinition Height="230"/>
            </Grid.RowDefinitions>

            <DataGrid Name="TodoTasksGrid"
                      Grid.Row="0"
                      ItemsSource="{Binding TodoTasks}"
                      SelectedItem="{Binding SelectedTask, Mode=TwoWay}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      GridLinesVisibility="All"
                      VerticalGridLinesBrush="LightGray"
                      HorizontalGridLinesBrush="LightGray"
                      PreviewMouseRightButtonDown="TodoTasksGrid_PreviewMouseRightButtonDown">
                <DataGrid.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Добавить" cal:Message.Attach="[Event Click] = [Action AddTask]"
                                  BorderBrush="{x:Null}" Background="White"/>
                        <MenuItem Header="Удалить" cal:Message.Attach="[Event Click] = [Action DeleteTask]"
                                  IsEnabled="{Binding CanDeleteTask}" BorderBrush="{x:Null}" Background="White"/>
                    </ContextMenu>
                </DataGrid.ContextMenu>
                
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Создана" Binding="{Binding CreatedAt, StringFormat='{}{0:d/MM/yyyy}'}" 
                                        Width="90"/>
                    <DataGridTextColumn Header="Важность" Binding="{Binding Importance}" Width="80"/>
                    <DataGridTemplateColumn Header="Задача" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}" TextWrapping="Wrap">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                        <Setter Property="TextDecorations" Value="Strikethrough"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    <ItemsControl ItemsSource="{Binding Subtasks}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock TextWrapping="Wrap">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                                    <Setter Property="TextDecorations" Value="Strikethrough"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                    <Run Text=" - "/>
                                                    <Run Text="{Binding Title}"/>
                                                </TextBlock>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Закрыта" Binding="{Binding CompletedAt, StringFormat='{}{0:d/MM/yyyy}'}" Width="90"/>
                    <DataGridTextColumn Header="Подзадачи" Binding="{Binding ProgressText}" Width="75"/>
                </DataGrid.Columns>
            </DataGrid>

            <GridSplitter Grid.Row="1" Height="5" HorizontalAlignment="Stretch"/>

            <Grid Grid.Row="2" Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="3*"/>
                </Grid.ColumnDefinitions>

                <GroupBox Grid.Column="0" Header="Задача" Padding="5">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="90"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Название:" VerticalAlignment="Top" Margin="0,3,5,3"/>
                        <TextBox Grid.Row="0" Grid.Column="1" 
                                 Text="{Binding SelectedTask.Title, UpdateSourceTrigger=PropertyChanged, NotifyOnSourceUpdated=True}" 
                                 MaxLength="1000" AcceptsReturn="True" TextWrapping="Wrap" Height="70" Margin="0,0,0,5"
                                 cal:Message.Attach="[Event SourceUpdated] = [Action MarkDirty]"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Важность:" VerticalAlignment="Center" Margin="0,3,5,3"/>
                        <ComboBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding ImportanceLevels}" 
                                  SelectedItem="{Binding SelectedTask.Importance, NotifyOnSourceUpdated=True}" Width="130" 
                                  HorizontalAlignment="Left"  Margin="0,0,0,5"
                                  cal:Message.Attach="[Event SourceUpdated] = [Action MarkDirty]"/>

                        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" Margin="0,5,0,0">
                            <TextBlock Text="Создана: " FontWeight="Bold" Margin="5,0,0,0"/>
                            <TextBlock Text="{Binding SelectedTask.CreatedAt, StringFormat='{}{0:d/MM/yyyy}'}" Margin="0,0,15,0"/>
                            <TextBlock Text="Закрыта: " FontWeight="Bold" 
                                       Visibility="{Binding SelectedTask.IsCompleted, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                            <TextBlock Text="{Binding SelectedTask.CompletedAt, StringFormat='{}{0:d/MM/yyyy}'}"
                                       Visibility="{Binding SelectedTask.IsCompleted, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                        </StackPanel>

                        <Button Grid.Row="3" Grid.Column="1" Content="{Binding TaskCompletionButtonCaption}" 
                                Name="ToggleTaskCompletion" IsEnabled="{Binding CanToggleTaskCompletion}"
                                Width="130" Height="24" HorizontalAlignment="Left" Margin="0,10,0,0"/>
                    </Grid>
                </GroupBox>

                <DockPanel Grid.Column="2">
                    <DockPanel DockPanel.Dock="Top" Margin="0,0,0,5">
                        <Button DockPanel.Dock="Right" Content="Удалить подзадачу" Name="DeleteSubtask" 
                                IsEnabled="{Binding CanDeleteSubtask}" Width="120" Height="24" Margin="5,0,0,0"/>
                        <Button DockPanel.Dock="Right" Content="↓" Name="MoveSubtaskDown" 
                                IsEnabled="{Binding CanMoveSubtaskDown}" Width="28" Height="24" Margin="5,0,0,0"/>
                        <Button DockPanel.Dock="Right" Content="↑" Name="MoveSubtaskUp" 
                                IsEnabled="{Binding CanMoveSubtaskUp}" Width="28" Height="24" Margin="5,0,0,0"/>
                        <Button DockPanel.Dock="Right" Content="Добавить подзадачу" Name="AddSubtask" 
                                IsEnabled="{Binding CanAddSubtask}" Width="130" Height="24"/>
                        <TextBlock Text="Подзадачи" FontWeight="Bold" VerticalAlignment="Center"/>
                    </DockPanel>

                    <DataGrid ItemsSource="{Binding Subtasks}"
                          SelectedItem="{Binding SelectedSubtask, Mode=TwoWay}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserSortColumns="False"
                          SelectionMode="Single"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="LightGray">
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="Готово" Width="60">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsCompleted, UpdateSourceTrigger=PropertyChanged}"
                                              HorizontalAlignment="Center"
                                              cal:Message.Attach="[Event Click] = [Action SubtaskCompletionChanged]"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Название" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Title}">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                        <Setter Property="TextDecorations" Value="Strikethrough"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                            <DataGridTemplateColumn.CellEditingTemplate>
                                <DataTemplate>
                                    <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged, NotifyOnSourceUpdated=True}"
                                             cal:Message.Attach="[Event SourceUpdated] = [Action MarkDirty]"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellEditingTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
                </DockPanel>
            </Grid>
        </Grid>
    </DockPanel>
</Window>
