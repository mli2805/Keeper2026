<UserControl x:Class="KeeperWpf.TwoSelectorsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:keeperWpf="clr-namespace:KeeperWpf"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance keeperWpf:TwoSelectorsViewModel}">
    <Border BorderBrush="Gray" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <keeperWpf:PeriodSelectControl 
                  Visibility="{Binding PeriodSelectControlVisibility}"  
                  SelectedPeriod="{Binding ShellPartsBinder.TranslatedPeriod, Mode=TwoWay}"
                  ChangeControlTypeEvent="{Binding IsPeriod, Mode=TwoWay}"
                  VerticalAlignment="Bottom" Grid.Row="0" Margin="0,5,0,10"/>
            <keeperWpf:DateSelectControl 
                  Visibility="{Binding DateSelectControlVisibility}"  
                  SelectedDay="{Binding ShellPartsBinder.TranslatedDate, Mode=TwoWay}"
                  ChangeControlTypeEvent="{Binding IsPeriod, Mode=TwoWay}"
                  VerticalAlignment="Bottom" Grid.Row="1" Margin="0,5,0,10"/>

        </Grid>
    </Border>
</UserControl>
