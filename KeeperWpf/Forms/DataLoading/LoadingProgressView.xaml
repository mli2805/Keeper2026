<Window x:Class="KeeperWpf.LoadingProgressView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Height="250" Width="500" WindowStartupLocation="CenterScreen">
    <DockPanel>
        <TextBlock DockPanel.Dock="Top" FontSize="16" Text="Загрузка данных из текстовых файлов." Margin="5,20,5,5" TextAlignment="Center"/>
        <TextBlock DockPanel.Dock="Top" FontSize="16" Text="{Binding WritingToDbMessage}" Margin="5,5,5,5" TextAlignment="Center"/>
        <DockPanel DockPanel.Dock="Bottom">
            <Button DockPanel.Dock="Right" Margin="5,5,5,20" Width="100" Content="Cancel" Name="Cancel" Height="22"/>
        </DockPanel>
        <ProgressBar DockPanel.Dock="Bottom" Height="5" Margin="20,-10,20,50" IsIndeterminate="True" 
                     Background="Lime" Foreground="White" BorderThickness="0"/>
        <TextBlock></TextBlock>
    </DockPanel>
</Window>
