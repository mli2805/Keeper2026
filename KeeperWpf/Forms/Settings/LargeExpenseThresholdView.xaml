<Window x:Class="KeeperWpf.LargeExpenseThresholdView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             Height="450" Width="600" WindowStartupLocation="CenterScreen"
             d:DataContext="{d:DesignData LargeExpenseThresholdViewModel}">
    <DockPanel>
    <DataGrid ItemsSource="{Binding KeeperDataModel.LargeExpenseThresholds}" AutoGenerateColumns="False" Margin="5" >
        <DataGrid.Columns>
            <DataGridTextColumn Header="#" Width="30" Binding="{Binding Id}"/>
            <DataGridTextColumn Header="С даты" Width="100" Binding="{Binding FromDate, StringFormat='dd/MM/yyyy'}"/>
            <DataGridTextColumn Header="Сумма (для месячного анализа)" Width="200" Binding="{Binding Amount}"/>
            <DataGridTextColumn Header="Сумма (для годового анализа)" Width="200" Binding="{Binding AmountForYearAnalysis}"/>
        </DataGrid.Columns>
    </DataGrid>
    </DockPanel>
</Window>
